name: demo2

on: 
    push: 
      branches:
            - main
        
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: checkout repo
              uses: actions/checkout@v4
            - name: print hello 
              run: echo "Hello from demo2!"
  